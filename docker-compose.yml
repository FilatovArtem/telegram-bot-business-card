services:
  bot:
    build: .
    env_file: .env
    volumes:
      - bot-data:/app/data
    restart: unless-stopped

  # Production variant with PostgreSQL
  # bot-pg:
  #   build: .
  #   env_file: .env
  #   environment:
  #     DATABASE_URL: postgresql+asyncpg://bot:bot@db:5432/bot
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #   restart: unless-stopped
  #
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: bot
  #     POSTGRES_PASSWORD: bot
  #     POSTGRES_DB: bot
  #   volumes:
  #     - pg-data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U bot"]
  #     interval: 5s
  #     timeout: 3s
  #     retries: 5

volumes:
  bot-data:
  # pg-data:
