## Что изменилось

(1-3 пункта)

## Почему

(контекст / ссылка на issue)

## Чек-лист

- [ ] `uv run ruff check .` зелёный
- [ ] `uv run ruff format --check .` зелёный
- [ ] `uv run mypy bot` зелёный
- [ ] `uv run pytest -v` зелёный
- [ ] Если добавлены модели — миграция Alembic создана
- [ ] CHANGELOG.md обновлён (если user-visible change)
